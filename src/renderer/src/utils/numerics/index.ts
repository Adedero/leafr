import { round } from "./lib/round";
import { isDecimal } from "./lib/is-decimal";
import { decimalPoints } from "./lib/decimal-points";

const Numerics = {
  decimalPoints,
  isDecimal,
  round
};

export { Numerics };
